# Decision records

Substantial decisions get a short record here: context, options, decision, reasoning,
sources. The specification carries the current state; these records carry the *why* and
the history. Format: `NNNN-short-title.md`.
